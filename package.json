{
  "name": "creativeclaw",
  "version": "0.1.0",
  "private": true,
  "description": "AI agent platform for creative professionals",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "dev": "npm run -w @creativeclaw/gateway dev",
    "test": "npm run --workspaces --if-present test",
    "lint": "echo 'lint placeholder'"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.3"
  }
}
